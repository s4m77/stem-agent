"""Finite workflow candidates for later search."""

CANDIDATE_WORKFLOWS = [
    "direct_code",
    "schema_then_code",
    "plan_then_code",
    "skill_retrieval_then_code",
    "code_then_verify",
]

